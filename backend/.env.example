# Server port (optional, defaults to 3000)
PORT=3000

# No API keys go here — this backend never stores or hardcodes provider
# keys. Each request supplies its own via the x-ai-provider / x-ai-key
# headers, coming from whatever the mobile app user configured.
